# Compose secrets — real values live here, never commit.
# Template is tracked as .env.compose.example.


# Local .env backups
*.env.bak.*
.env.bak.*

# Dependencies / build output (sub-projects also ignore these)
node_modules/
build/
dist/
coverage/

# Logs
logs/
*.log

# Tooling caches
.scannerwork/
.eslintcache
